# 复制为 .env 并填写你的实际值
# 委派给本服务的权威子域（末尾不带点）
PROBE_ZONE=dns-probe.example.com
# A 记录返回的 IPv4，一般填 VPS 公网 IP
PROBE_ANSWER=203.0.113.10
# 可选：覆盖镜像地址/标签（默认 ghcr.io/palemoky/dns-probe:latest）
# DNS_PROBE_IMAGE=ghcr.io/palemoky/dns-probe:latest
